# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.venv/
venv/

# uv
# uv.lock 不入库: 本机 uv 配置了清华镜像, 每次 lock 都会改写全部源 URL,
# 提交会污染协作者/CI 的 lockfile。版本 pin 由 pyproject.toml 约束。
uv.lock
backend/uv.lock

# Env / secrets
# /.env: 根目录部署凭据 (docker compose 插值用); backend/.env: 后端本地开发
.env
.env.*
!.env.example
backend/.env
frontend/.env

# Test / lint / type-check caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# Logs / temp / runtime artifacts
*.log
logs/
*.tmp
*.bak
*.orig
backend/import_log.txt
backend/import_err.txt

# SQLite (legacy)
*.db

# IDE
.idea/
.vscode/
.DS_Store

# Frontend
node_modules/
dist/

# Jupyter
.ipynb_checkpoints/

# Data exports from notebooks
exports/

# Local agent memory (handover notes, preferences)
memory/

# Claude worktrees
.claude/worktrees/
